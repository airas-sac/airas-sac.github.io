# AIRAS website

Edit the main page content in `index.md`.

GitHub Pages builds the Markdown file with the layout in `_layouts/default.html`.
The site styling and navigation behavior remain in `styles.css` and `script.js`.
